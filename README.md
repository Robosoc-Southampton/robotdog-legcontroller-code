# Robot Dog Leg Controller Code

- Compile with `make`
- Upload to microcontoller with `make install`
- Generate assembly code with `make asm`
- Set up the microcontroller by calling the `mcu-setup` script
